name: sub

on:
  workflow_call:
    inputs:
      user_name:
        description: 배포한 사용자
        required: true
        type: string
      stage:
        type: string
      branch:
        description: 브랜치
        type: string

jobs:
  sub:
    runs-on: ubuntu-latest
    name: abcd ${{ inputs.stage }} / ${{ inputs.branch || github.ref_name }}

    steps:
      - run: |
          echo ${{ inputs.stage }}
          echo ${{ inputs.user_name }}
          echo ${{ inputs.branch || github.ref_name }}
